# 📘 HTML Complete Cheat Sheet & Fundamentals Guide

This repository contains a **comprehensive HTML reference and cheat sheet** designed to provide a complete overview of **HTML structure, tags, attributes, and HTML5 elements**.  
It serves as a **quick-reference guide as well as a learning resource** for students, beginners, and developers who want to understand or revise core HTML concepts efficiently.

The content is structured in a simple, easy-to-understand format, making it ideal for both **learning and revision purposes**.

---

## 📖 About This Repository

HTML (HyperText Markup Language) is the backbone of the web. Every website relies on HTML to define its structure and content.  
This repository compiles **all essential HTML concepts in one place**, covering everything from basic document structure to advanced HTML5 semantic elements.

It is especially useful as:
- A **cheat sheet** before exams or interviews
- A **reference guide** during web development
- A **learning companion** for beginners

---

## 🎯 Who This Repository Is For

This repository is ideal for:
- Beginners learning **HTML from scratch**
- Computer Science and Engineering students
- Frontend developers revising fundamentals
- Anyone preparing for **web development interviews**
- Learners who prefer concise, structured explanations

No prior web development experience is required.

---

## 🧠 What You Will Learn

By using this repository, you will gain a strong understanding of the following HTML concepts:

---

## 🏗️ HTML Document Structure

- `<html>` – Root element of an HTML document  
- `<head>` – Metadata, title, styles, scripts, and SEO-related information  
- `<title>` – Webpage title shown in browser tabs  
- `<body>` – Visible content of the webpage  

You will understand how a complete HTML page is structured from top to bottom.

---

## 🧾 Metadata & Head Elements

- `<meta>` – Character set, author, keywords, and page metadata  
- `<base>` – Base URL for relative links  
- `<link>` – External stylesheets and resources  
- `<style>` – Internal CSS styling  
- `<script>` – JavaScript integration  

These elements help control page behavior, styling, and compatibility.

---

## ✍️ Text Formatting & Typography

Learn how to format and emphasize content using:
- `<strong>`, `<b>` – Bold text  
- `<em>`, `<i>` – Italic text  
- `<small>`, `<mark>` – Highlighting and small text  
- `<sub>`, `<sup>` – Subscript and superscript  
- `<code>`, `<pre>` – Code formatting  
- `<blockquote>`, `<q>`, `<cite>` – Quotes and citations  

---

## 🧩 Headings, Layout & Containers

- `<h1>` to `<h6>` – Headings hierarchy  
- `<div>` – Block-level container  
- `<span>` – Inline container  
- `<p>` – Paragraphs  
- `<br>`, `<hr>` – Line breaks and separators  

These tags form the layout and visual structure of a webpage.

---

## 🖼️ Images & Media

- `<img>` – Display images  
- Image attributes: `src`, `alt`, `height`, `width`, `border`, `align`  
- Image maps using `<map>` and `<area>`  

You will learn how to embed and customize images effectively.

---

## 🔗 Links & Navigation

- `<a href="">` – Hyperlinks  
- Email links using `mailto:`  
- Phone links using `tel:`  
- Page navigation using anchor names and IDs  

These tags enable navigation across pages and within a webpage.

---

## 📋 Lists

- `<ol>` – Ordered lists  
- `<ul>` – Unordered lists  
- `<li>` – List items  
- `<dl>`, `<dt>`, `<dd>` – Definition lists  

Lists help organize content clearly and semantically.

---

## 📝 Forms & User Input

Learn how to collect user data using:
- `<form>` – Form container  
- `<input>` – Text, password, email, date, etc.  
- `<textarea>` – Multi-line text input  
- `<select>`, `<option>` – Dropdowns  
- `<button>` – Form submission  

Form attributes include:
- `action`, `method`, `required`, `placeholder`, `pattern`, `min`, `max`, `disabled`, `autofocus`

---

## 📊 Tables

- `<table>` – Table structure  
- `<thead>`, `<tbody>`, `<tfoot>` – Table sections  
- `<tr>`, `<th>`, `<td>` – Rows, headers, and cells  
- `<colgroup>`, `<col>` – Column styling  

Tables are useful for displaying structured data.

---

## 🎥 Multimedia & Embedded Content

- `<object>` – Embed multimedia or documents  
- `<iframe>` – Inline embedded content  
- `<embed>` – External plugins and media  

You will learn how to embed videos, PDFs, and external pages.

---

## 🧠 HTML5 Semantic Elements

Modern HTML5 tags covered include:
- `<header>`, `<footer>`  
- `<main>`, `<section>`, `<article>`  
- `<aside>`, `<nav>`  
- `<figure>`, `<figcaption>`  
- `<details>`, `<summary>`  
- `<dialog>`, `<time>`, `<progress>`, `<meter>`  

These elements improve accessibility, SEO, and code readability.

---

## 🔣 HTML Character Entities

Commonly used HTML entities:
- `&lt;` `<`  
- `&gt;` `>`  
- `&quot;` `"`  
- `&nbsp;` Non-breaking space  
- `&copy;` ©  
- `&amp;` &  
- `&trade;` ™  

Essential for handling special characters safely.

---

## 🚀 Why Use This Repository

- ✔️ Covers **complete HTML fundamentals**
- ✔️ Easy-to-read cheat sheet format
- ✔️ Ideal for **quick revision**
- ✔️ Helpful for **students and interview prep**
- ✔️ Supports modern **HTML5 standards**

---

## 📚 Purpose of This Repository

This repository is maintained as:
- A **learning resource** for HTML beginners  
- A **revision guide** for students and developers  
- A **reference document** during web development  

---
